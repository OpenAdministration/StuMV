; Raised for realm branding uploads (background photos in particular can
; easily be several MB straight out of a camera/phone) and to give GD enough
; headroom to decode+resize large images without hitting memory exhaustion.
upload_max_filesize = 12M
post_max_size = 15M
memory_limit = 256M
